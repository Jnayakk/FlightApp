<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="cs.b07.cscb07courseproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ClientMainActivity" />
        <activity android:name=".AdminMainActivity" />
        <activity android:name=".AccountSettingActivity" />
        <activity android:name=".SearchActivity" />
        <activity android:name=".CreatePassActivity" />
        <activity android:name=".FlightsActivity" />
        <activity android:name=".CreateClientActivity" />
        <activity android:name=".ItineraryActivity" />
        <activity android:name=".ViewClientsActivity" />
        <activity android:name=".ViewAllFlightsActivity" />
        <activity android:name=".EditFlightActivity" />
        <activity android:name=".ItineraryIndividualActivity" />

        <meta-data
            android:name="com.google.android.gms.version"
            android:value="@integer/google_play_services_version" />

        <activity android:name=".BookedActivity" />
        <activity android:name=".IndividualBookedItineraryActivity"></activity>
    </application>

</manifest>